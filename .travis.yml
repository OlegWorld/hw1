language: cpp
before_script:
- sudo apt-get install libboost-test-dev -y
script:
- cmake .
- cmake --build .
- cmake --build . --target test
- cmake --build . --target package
deploy:
    provider: script
    skip_cleanup: true
    script:
    - curl -T hw1-0.1.$TRAVIS_BUILD_NUMBER-Linux.deb -olegworld:$BINTRAY_API_KEY "https://api.bintray.com/packages/olegworld/Otus_Homework/hw1/$TRAVIS_BUILD_NUMBER/hw1-0.1.$TRAVIS_BUILD_NUMBER-Linux.deb;deb_distribution=trusty;deb_component=main;deb_architecture=amd64;publish=1"
